# Copy this to .env and fill in with your database details
# Option 1: Direct Postgres connection (used by psycopg2)
DATABASE_URL=postgresql://<user>:<pass>@<host>:5432/<db_name>?sslmode=require

# Option 2: Use Supabase server-side client (recommended for Supabase projects)
# SUPABASE_URL=https://your-project-ref.supabase.co
# SUPABASE_SERVICE_KEY=sb_secret_...   # keep this secret; do NOT commit

# Choose one of the above options and create a file named .env in this folder with the real values.
